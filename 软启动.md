# 软启动

- CPU 复位正常后，表示硬启动完成，CPU 开始工作，进行软启动。

## 软启动流程

- 1、CPU 会通过 DMI 总线向 PCH 桥发送寻址指令
- 2、PCH 桥收到 CPU 的寻址指令后开始寻找 BIOS
- 3、首先搜寻 SPI 总线上是否有 BIOS,当 SPI 总线上没有 BIOS，根据 SPI 总线信号设置来判断 BIOS 在什么地方
- 4、如果 BIOS 挂在 EC 下，桥通过 LPC 总线上的 EC 通信，当 EC 收到寻址指令后再经过 SPI 总线读取 BIOS。BIOS 再原路返回数据到 CPU，CPU 运行 BIOS 中的 POST 自检程序，开始自检动作。
- 5、CPU 正确读取到 BIOS 自检程序后，开始执行 POST 指令。

###

- DMI 总线一般走中间板层，没法测
- 内存的 SMBus 总线
- 内存 SPD 信息 只需要给 SPD 供电就能读取

* 在 CPU 内部有专门用来管理内存的模块，我们叫内存控制器

  - 内存控制器启动条件 VDDQ(旧机器) ，VDD2(新机器) 供电

* 内存自检不过
  - 自身工作条件缺少
  - CPU 内存管理模块有问题自检不过
  - 桥读不到内存 自检不过
  - 内存总线有问题 自检不过
  - PCH CPU BIOS 内存
* 能做的只有 刷 BIOS

### 分辨桥读 BIOS 还是 CPU 读 BIOS

- 老机器通过桥来读屏 新机器由 CPU 读屏

* 读屏后
  - BKLTEN 背光开启 3.3V
  - VDDEN 屏供电开启 3.3V
  - BKLTCTL 亮度调节 键盘快捷键 0~3V 之间可调 2.8V 正常亮度
  - LVDS 显示信号 （新机器 EDP_TX）
